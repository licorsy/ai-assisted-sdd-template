---
description: Start or resume the orchestrated roadmap session - confirms the Step 0 startup choices and current phase, then drives phase-gated work per the orchestrator prompt.
argument-hint: [reset]
---

Follow `agents/orchestrator.md` exactly, in full, as your controlling context for the rest of this session. Read that file first.

Then:

1. Read `/docs/status.md` if it exists, to recover what is being brought, the starting condition, roadmap path, interaction level, phase-exit brief format, and current phase already recorded.
2. If `$ARGUMENTS` contains "reset", or `/docs/status.md` does not exist yet, or it does not record these choices, run Step 0 of the orchestrator prompt: ask the human what they are bringing (a problem, an idea, an already-thought-out solution, or a ready document), then the starting condition (greenfield or brownfield), the roadmap path (full roadmap, short path, or fast-track), the interaction level (full-gate, phase-gate, or autonomous-with-report; default full-gate), and the phase-exit brief format (free-prose or standardized six-field brief; default free-prose).
3. Run the Step 2 session protocol: confirm the current phase, summarize the expected artifacts, identify the smallest useful next step, and ask whether to proceed.
4. For the rest of this session, keep following the orchestrator prompt's operating rules, the summarize-and-confirm rule, and the readiness checklist for every phase transition.
5. If asked to visualize current status, you can render `/docs/status.md`/`docs/STATE.md` as an HTML Artifact dashboard - see `docs/manuals/role-operating-guide.md`, Section 18.
